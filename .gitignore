# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env

# Catalog product images (regenerated from PDF; not committed — see
# src/assets/catalogs/valvulas/WHERE_IMAGES.txt)
public/catalogs/products/

# Zip bundle for manual upload to Blob / build agent (see npm run catalog:prepare-images)
catalog-export/

# Local log dumps (not app config)
TI_water_api/src/config/logs_api.yaml

# API-side intermediate JSON seed copies (SQL migrations are source of truth)
TI_water_api/catalog-seed/

# Generated catalog product payloads (source PDFs/SQL migrations are canonical)
src/assets/catalogs/**/product.json
src/assets/catalogs/**/BATCH-MANIFEST.json
src/assets/catalogs/*.pdf
